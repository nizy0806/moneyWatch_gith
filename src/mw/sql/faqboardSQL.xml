<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="faqboard">

	<insert id="insertBoard" parameterType="faqboardDTO">
		insert into mwFaQBoard values(mw_deleteMember_seq.nextval,#{faq_num},#{writer},#{subject},#{faq_eamil},#{content},#{pw},#{readcount},#{re_step},#{re_level},sysdate,#{ref})
	</insert>
	
	<!-- <select id="selectBoard" resultType="String" parameterType="faqboardDTO" >
		"select * from (select #{faq_num},#{writer},#{email},#{subject},#{pw},#{reg_date},#{re_step},#{re_level},#{content},#{readcount},rownum r from "+
		"(select *  from mwFaQBoard order by #{ref} desc, #{re_step} asc) order by #{ref} desc, re_step asc ) where r >= ? and r <= ? ")
	</select>	 -->		
</mapper>

